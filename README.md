# Directory Synchronizer

## Usage

```bash
dotnet run <source dir> <replica dir> <sync interval in seconds> <log file>
```

### Example

```bash
dotnet run ./source ./replica 10 ./logs/sync.log
```

## Stopping

Press `Ctrl+C` to stop the synchronization process.
